# Your photos

Upload JPG, PNG or WebP images here with Add file > Upload files.

In app/content.js, use a path like `/photos/first-date.jpg` for a memory, or set `closingPhoto` to `/photos/us.jpg`. Do not include `public` in the path.
